<% @resources.each do |post| %>
  <%= render Card.new do |c| %>
    <% c.with_head { post.title } %>
    <% c.with_body { post.body } %>
  <% end %>
<% end %>

<%== pagy_nav(@page) if @page.pages > 1 %>