<%= content_tag :aside, html_options do %>
  <%= with_head if head? %>
  <%= with_body do %>
  <%#= with_body context(:head) do %>
    <%= content %>
  <% end %>
  <%= with_foot if foot? %>
<% end %>
