<%= content_tag (order? ? :ol : :ul), options do %>
  <% if content? %>
    <% items.each do |item| %>
      <%= concat item %>
    <% end %>
  <% else %>
    <% (context || []).each do |c, o| %>
      <%= concat(with_item(c, **o)) %>
    <% end %>
  <% end %>
<% end %>
